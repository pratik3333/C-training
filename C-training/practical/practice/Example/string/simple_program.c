#include <stdio.h>
#include <conio.h>

int main(int argc, char const *argv[])
{
    char ch[50];
    printf("Enter the string data: ");
    scanf("%s", &ch);
    printf("\n Name is %s\n", ch);
    return 0;
}